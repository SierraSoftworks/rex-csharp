dist: xenial
addons:
  snaps:
  - name: dotnet-sdk
    classic: true
    channel: latest/beta
sudo: required
language: csharp
mono: none
before_script:
  - sudo snap alias dotnet-sdk.dotnet dotnet
  - dotnet --version
script:
  - dotnet restore
  - dotnet build
  - dotnet test
